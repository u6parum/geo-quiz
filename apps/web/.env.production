# В production Vite соберёт статику и может использовать относительный путь
# Если фронтенд и API на одном домене через nginx — base URL пустой
VITE_AUTH_API_URL=
VITE_GAME_API_URL=
VITE_API_BASE_URL=/api